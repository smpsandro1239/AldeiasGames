/**
 * Landing Page Route
 * Página pública de marketing
 */

import { LandingPage } from '@/components/landing/LandingPage';

export default function LandingPageRoute() {
  return <LandingPage onLoginClick={() => {}} onRegisterClick={() => {}} />;
}
